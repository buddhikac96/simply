// keyboard input
//get keyboardIn;
get maths;

function start(in: ) out:no {
    list integer b = [1,2,3,4,5,6];
    add(b);
    integer c = maths.power(2,3);
    display(c);
    display(maths.power(3,2));
}

function add(in: list integer a) out:no {
    repeat(integer i of a){
        display(i);
    }
}